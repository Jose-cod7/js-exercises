function s(w1, w2) {
    return w1.concat(w2);
}

var result = s("code", "your");
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them. concatenate the two words and log it.
// - How many parameters does the function take? Take two parameters.
// - What is the function name? the function name is s.
// - Where is the function called? with what parameters? Line 5 with 'code' and 'your'
// - What does the function return? concatenate the two parameters.
// - How can this function be improved?